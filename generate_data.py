'''
Generates an NPZ file of (X, Y) pairs. Must specify the task in tasks/.

python generate_data.py --task <TASK> --samples <SAMPLES>  --filename <FILENAME>
    task: the task located in tasks/
    samples: number of samples for dataset
    filename: name of npz file to be located in the datasets/ folder.
'''